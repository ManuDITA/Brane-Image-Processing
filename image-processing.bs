import images_io;
import image_filters;

#[on("localhost")]
{
    println("Hello, world!");
    
    let grayimage_dataset := grayscale(new Data{ name := "data_images" });
    let blurimage_dataset := blur(grayimage_dataset, 15);
    

    commit_result("outputdata", blurimage_dataset);
    return "Finish";
}